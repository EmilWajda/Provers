[project]
name = "loft-core"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=24.1.0",
    "quart>=0.20.0",
    "quart-cors>=0.8.0",
]

[tool.setuptools]
packages = ["loft"]
